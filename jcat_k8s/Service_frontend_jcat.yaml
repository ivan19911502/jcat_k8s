apiVersion: v1
kind: Service
metadata:
  name: jcat-frontend-service
  namespace: __NAMESPACE__
spec:
  selector:
    app: frontend
  ports:
  - port: 81
    targetPort: 3000
    nodePort: 30080 
  type: NodePort
